package applygray.mattermanipulator.building;

/** The explicit no-extra-state payload used by ordinary block captures. */
public enum EmptyCapturedBlockData implements CapturedBlockData {

    INSTANCE
}
